services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    image: rxflow-client:latest
    container_name: rxflow-client
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=production
    restart: unless-stopped
